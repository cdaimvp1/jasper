# Real business data - NEVER commit (this repo is public).
data/
new_cohort/
scratch/
*.db
*.db-*
*.db.bak*
*.sqlite*
*.log

# Local/session/identity config - environment-specific, not portable source.
.claude/
config/
workers/*/settings.json
workers/*/*.md
workers/*/memory/
workers/*_active.md
setup/_backups/

# Raw scan/ingest output - real email content, names, addresses. Never source.
ingest/scan_wide.jsonl
*.jsonl
setup/_diff_*.txt
setup/_refresh_live_run.log
setup/relay_status.txt
setup/_roster_add_*.json
relay_hello.txt

# Python
__pycache__/
*.pyc
.pytest_cache/
tests/_pytest_scratch/

# OS/editor cruft
.DS_Store
Thumbs.db

# Ad-hoc analysis scratch produced while investigating live data (tasks #413-#418
# and predecessors). These hold REAL calendar/mail content - names, addresses,
# suppliers - so they must never reach this public repo. Durable findings belong
# in docs/design/ROADMAP.md; the raw files belong in scratch/ (already ignored).
_*.json
_*.py
_*.txt
.relay_*.json
.synthesis_scratch/
